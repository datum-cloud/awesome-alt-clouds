# Runtime deps for scripts/ (submission pipeline, profile generation, data regen)
# Local install (no venv): python3 -m pip install --user --break-system-packages -r requirements.txt
requests>=2.31.0
beautifulsoup4>=4.12.0
openai>=1.0.0
